<%= form_with(model: position, local: true) do |form| %>
  <% if position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(position.errors.count, "error") %> prohibited this position from being saved:</h2>

      <ul>
      <% position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :position_title %>
  </div>

  <div class="field">
    <%= form.label :created_at %>
    <%= form.text_field :created_at, id: :position_created_at %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location, id: :position_location %>
  </div>

  <div class="field">
    <%= form.label :kind %>
    <%= form.text_field :kind, id: :position_kind %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :position_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
